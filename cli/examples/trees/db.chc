% Binary tree operations using functor notation.
% A tree is either [] (leaf) or bin(L, R) (internal node with subtrees L and R).

tree([]).
tree(bin(X, Y)) :- tree(X), tree(Y).

in(X, X).
in(X, bin(Y, _)) :- in(X, Y).
in(X, bin(_, Y)) :- in(X, Y).
