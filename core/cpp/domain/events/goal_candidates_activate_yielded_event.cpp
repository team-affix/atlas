#include "../../../hpp/domain/events/goal_candidates_activate_yielded_event.hpp"

std::ostream& operator<<(std::ostream& os, const goal_candidates_activate_yielded_event&) {
    return os << "goal_candidates_activate_yielded_event{}";
}
