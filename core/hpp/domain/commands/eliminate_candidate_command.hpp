#ifndef ELIMINATE_CANDIDATE_COMMAND_HPP
#define ELIMINATE_CANDIDATE_COMMAND_HPP

#include "../value_objects/lineage.hpp"

struct eliminate_candidate_command {
    const resolution_lineage* rl;
};

#endif
