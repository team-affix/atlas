#ifndef GOAL_CANDIDATES_DEACTIVATE_YIELDED_EVENT_HPP
#define GOAL_CANDIDATES_DEACTIVATE_YIELDED_EVENT_HPP

#include <ostream>

struct goal_candidates_deactivate_yielded_event {};

std::ostream& operator<<(std::ostream&, const goal_candidates_deactivate_yielded_event&);

#endif
