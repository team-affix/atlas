#ifndef ELIMINATE_CANDIDATE_YIELD_EVENT_HPP
#define ELIMINATE_CANDIDATE_YIELD_EVENT_HPP

#include <ostream>

struct eliminate_candidate_yield_event {};

std::ostream& operator<<(std::ostream&, const eliminate_candidate_yield_event&);

#endif
