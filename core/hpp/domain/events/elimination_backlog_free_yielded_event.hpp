#ifndef ELIMINATION_BACKLOG_FREE_YIELDED_EVENT_HPP
#define ELIMINATION_BACKLOG_FREE_YIELDED_EVENT_HPP

#include <ostream>

struct elimination_backlog_free_yielded_event {};

std::ostream& operator<<(std::ostream&, const elimination_backlog_free_yielded_event&);

#endif
