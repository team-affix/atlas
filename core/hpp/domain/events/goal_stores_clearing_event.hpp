#ifndef GOAL_STORES_CLEARING_EVENT_HPP
#define GOAL_STORES_CLEARING_EVENT_HPP

#include <ostream>

struct goal_stores_clearing_event {
};

std::ostream& operator<<(std::ostream&, const goal_stores_clearing_event&);

#endif
