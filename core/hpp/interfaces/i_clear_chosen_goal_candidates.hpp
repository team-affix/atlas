#ifndef I_CLEAR_CHOSEN_GOAL_CANDIDATES_HPP
#define I_CLEAR_CHOSEN_GOAL_CANDIDATES_HPP

struct i_clear_chosen_goal_candidates {
    virtual ~i_clear_chosen_goal_candidates() = default;
    virtual void clear() = 0;
};

#endif
